---
id: day03
layout: default
title: Day 3 Recap Questions
reference solutions:
  1. [NEW] Trick question! There is no one best editor. Two of the most popular ones to use in the terminal are `emacs` and `vim`. You can also use `nano` as a simpler alternative. Many students also find VS Code useful. If you're interested, check out the "Setting up VS Code" link on the course website (under Day 3 Additional Resources.)
  2. Allows us to record "snapshots" of our code and to return to those snapshots if we introduce bugs or other problems. Also allows us to share code with collaborators.
  3. clone, pull, commit, push, add, mv, rm, status
  4. [NEW] A commandline tool to copy files securely across computers. With the `scp` command on unix systems, including MacOS and Windows Powershell, or with the 'pscp' command on Windows if Putty is installed. 
  5. [NEW] A single file, sometimes referred to as an archive, bundling together more than one file, and potentially, folders. With the `zip` command.
  6. Your submission must include your source code .c files and your gitlog.txt bundled in a single zip file. This file should be downloaded to your personal computer from ugrad through secure copy and then submitted through Gradescope.
---

1. What is the best text editor to use in the terminal? Name two popular choices.
2. Why do we use version control system like **git**?
3. Name six common **git** commands?
4. What is secure copy? How do we invoke it?
5. What is a zip file? How do we create one?
6. What are the files that must be included in your submission? How do you obtain them and where do you submit them?
